import Navigation from "../components/Navigation";

const Home = () => {
    return (
        <section>
            <Navigation />
            <main className="home bg-img-custom">
                <div className="logo-section bg-img-custom"></div>
            </main>
        </section>
    );
}

export default Home;